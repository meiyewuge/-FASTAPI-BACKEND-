# V4 P1 · L2/L3/L4 交付报告

> 阶段：开发前置设计（**不写业务代码、不改 production、不触发火山、不大文件压测**）。
> 依据：`CLAUDE_L2L3L4_PROMPT.md`（精细版）+ 仓库 `claude/v4-staging` 真实代码 / DB schema / API / 权限。
>
> **📌 修订说明（第二轮）**：本三件套已按 ChatGPT 第一轮审核意见修订，详见 **`V4_P1_L2L3L4_REVIEW_FIX_REPORT.md`**。主要变更：A台主入口统一为 `/api/compose`；B台标准字段 `source_video_ids` + 会话源池三层优先级；30s 硬门槛；命名统一 `duration_seconds`；回填脚本改为强制；90–120s 列 P1-B。本文件下方第 8 节「待拍板点」中第 1/2/4/5 项多已被 ChatGPT 决议固化，最新待拍板见修订报告末尾。

## 1. 三份文件是否已生成 —— ✅ 全部生成

| 交付物 | 文件路径（仓库 `backend/`） | 可下载 |
|----|----|----|
| L2 可点击原型 | `backend/V4_P1_WIREFRAME_PROTOTYPE.html` | ✅（浏览器直接打开） |
| L3 交互规格 | `backend/V4_P1_INTERACTION_SPEC.md` | ✅ |
| L4 技术设计 | `backend/V4_P1_TECHNICAL_DESIGN.md` | ✅ |
| 本报告 | `backend/V4_P1_L2L3L4_DELIVERY_REPORT.md` | ✅ |

## 2. commit 号
- 本次交付 commit：见推送结果（提交信息：`docs(L2L3L4): V4 P1 开发前置设计三件套 + 交付报告`）。
- 分支：`claude/v4-staging`（已推送 origin）。

## 3. L2 是否可点击 —— ✅ 是
- 纯单文件 HTML（无外部 CDN，离线可开），含 JS 交互：
  - 顶部存储栏 + **身份切换器**（user / invite_admin / super_admin）演示 `scope=tenant|global` 与候选池可见性差异。
  - 单框工作流：需求输入框 + **仅 📷图片/📎文件/🎬视频 三入口（无文本入口）** + 已上传素材栏（缩略图带 ✕）+ **底部仅 A台/B台 两按钮（无蓝色上传素材按钮）**。
  - 可点击弹窗 5 个：A台费用确认、A台生成中（第N段/拼接）、裂变门槛不足、裂变进行中（N/M）、反馈。
  - 母视频/源视频陈列面、裂变视频陈列面（播放/下载/删除/反馈，无勾选框）、回流候选池（仅 super_admin）。
  - **每个按钮旁灰字标注对应 API**（21 处 API 标注）。
  - 交互真链路演示：上传视频→进母视频面；A台确认→生成→进母视频面；B台门槛校验（<3 或 <30s 弹门槛）→满足则裂变 30/40/50 条→进裂变面；删除/反馈/审核动作。

## 4. L3 是否覆盖控件状态机 —— ✅ 是
- **按钮级状态机**：A台按钮（默认/禁用/确认中/生成中/拼接中/完成/失败）、B台按钮（默认/禁用·数量/禁用·时长/加载/运行/完成/部分失败）、上传入口、视频卡片、候选池条目。
- **完整异常矩阵**：网络断/超时/401/403/跨租户403/413/格式/A台余额不足/A台超额/B台门槛/B台失败/track失败/feedback失败/审核失败——含「是否阻断」。
- **完整边界**：0/1/2/3 个视频、3个但1个<30s、正好3、4、5+、>2G、并发10、上传中断。
- **弹窗文案逐字写出**（A台费用确认、裂变门槛、裂变进行中、A台生成中、余额不足），无「参考某某」。
- **每页三态**（加载/空/错误）齐全。

## 5. L4 是否自包含 —— ✅ 是
- 面向未读过 V4 代码的模型（Qoder/Kimi/智谱），不依赖「你已知」。
- 含 **11 张表完整 CREATE TABLE DDL**（tenants/stores/videos/tasks/cost_records/uploads/invite_codes/admin_users/workflow_runs/video_feedback_signals/knowledge_candidates）。

## 6. 是否包含 DB / API / 权限 / 调度 / 部署 / 回滚 —— ✅ 全包含
- **DB**：完整 schema + P1 变更 SQL（`videos.duration`）+ 回滚 SQL + 数据迁移（ffprobe 回填）说明。
- **API**：B台自动选源新规格、A台权限澄清、compose 底座、uploads/videos/delete/storage/回流接口完整规格（含错误码/异步/花钱/进候选池）。
- **权限调整对照表**：明确 A台/B台**当前已是 require_auth**，P1 无破坏性守卫变更（卡控取消主要在前端）。
- **调度/逻辑**：B台裂变伪代码（自动选源+1:10+30/40/50）、A台一键成片伪代码（多段15s+concat）、ffmpeg 时长控制参数。
- **部署清单**（扣子 12 步）+ **回滚方案**（后端/DB/前端）。
- **范围划分**：本轮 / P1-B / P2 三档清单。

## 7. 是否可以交 ChatGPT 审核 —— ✅ 可以
三份文件自洽、互相引用、与真实代码对齐，可整体交 ChatGPT 审核「逻辑/页面/接口/数据库/权限/部署闭环」。下载方式：从 `claude/v4-staging` 拉取 `backend/V4_P1_*` 四个文件。

## 8. 仍需吴哥拍板的点（重要）
1. **A台单价文案**：L2/L3 用了占位「¥1.50/条」「余额 ¥98.50」。真实单价应来自 cost_engine 计价（按秒×分辨率），前端展示口径请确认（固定提示 or 实时估算）。
2. **A台权限事实澄清**：后端 A台**从来就是登录即用（require_auth）**，并非 require_super_admin。所以「取消授权卡控」在后端**无守卫可改**，只需前端去掉「联系管理员」文案 + 加费用确认。请确认这一事实口径，避免 ChatGPT 误判为后端漏改。
3. **B台裂变成片目标时长 90–120s**：归入 **P1-B**（remixer ffmpeg `-t` 调整），是否要并入本轮一起做？
4. **源视频「≥30s 建议」是硬门槛还是软提示**：当前设计为**硬门槛**（<30s 不计入合格源、不足3个禁用 B台）。若希望 <30s 也能裂变（仅提示），需调整规则。
5. **`videos.duration` 回填**：存量库是否需要回填脚本（ffprobe 扫历史文件）？还是只对新数据写时长、存量按 NULL 处理（NULL 不计入合格源）？
6. **A台图生视频**：参考图真正进入火山图生视频属 P1-B（需真实 key 联调），本轮仅透传记录。确认是否本轮只做「文字→多段→拼接」。
7. **候选池审核范围**：当前 super_admin 平台级跨租户审核（策展大库需要）。确认是否维持平台级，而非按租户隔离。

---

### 约束遵守自查
- ✅ 未写业务代码（仅文档 + 可点击原型 HTML）
- ✅ 未改 production、未触发火山 API、未做大文件压测
- ✅ 未泄露 JWT_SECRET / ADMIN_KEY / VIDEO_API_KEY（仅在部署清单提示「不覆盖」）
- ✅ 三份文件 + 报告推送 `claude/v4-staging`
