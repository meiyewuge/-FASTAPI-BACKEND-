# QODER_QUEST_PROMPT.md｜可直接复制给 Qoder 的 Quest 指令

## 任务一：部署跑通

```text
我要部署一个项目，项目名叫《门店经营陪跑系统 MVP V0.1》。这是一个 H5前端 + FastAPI后端 + PostgreSQL + Redis + Docker Compose 的项目。

请你先读取根目录 AI_INSTRUCTIONS.md、README.md、QODER_QUICKSTART.md，然后按以下目标执行：

1. 不要重构项目。
2. 不要改成微信小程序。
3. 先保证 docker compose 能启动。
4. 启动后告诉我前端地址和后端接口文档地址。
5. 如果报错，只修复启动相关问题。
6. 保持评分逻辑由后端规则引擎计算，不要交给大模型。

启动命令：
cp .env.example .env
docker compose up -d --build

验收标准：
前端可访问：http://localhost:8080
后端可访问：http://localhost:8000/docs
```

## 任务二：修复和联调

```text
请帮我测试完整业务流程：
1. 提交首次诊断表单；
2. 查看首次诊断结果；
3. 提交月度体检表单；
4. 查看月度体检结果；
5. 查看趋势页；
6. 查看后台列表。

如果有接口错误或页面错误，请修复。不要大改项目结构。
```

## 任务三：移动端美化

```text
请优化前端移动端体验，要求：
1. 首页更像一个美业经营诊断工具；
2. 表单步骤清晰；
3. 分数和风险展示醒目；
4. 结果页适合门店老板阅读；
5. 保留现有接口路径；
6. 不要修改后端评分逻辑。
```

## 任务四：新增微信小程序前端，保留原项目

```text
请在当前项目基础上新增微信小程序原生前端，目录为 mini-program/。
要求：
1. 不删除现有 frontend/ H5项目；
2. 小程序调用现有 backend API；
3. 页面包括：首页、首次诊断、月度体检、结果页、个人中心；
4. 暂时不用微信云开发，先复用现有后端；
5. 给出小程序配置和运行说明。
```

