# V1.3A.9 R7 → R8 DIFF

Independent final audit + fix by Claude Code. Only the evidence/detector/build layer
was changed; frozen production code (`app/daily_loop/services/*`, migration SQL,
`tests/*` business assertions, restore fault-injection semantics, vendor) is untouched.

## Changed files

| File | Change |
|------|--------|
| `run_truth_gate.py` | Rewritten as structured detectors returning `{status, exit_code, failure_codes, details, executed_checks}` with a 0/1/2/3 exit taxonomy. No detector converts an unexpected exception into a security FAIL (P0-7/8/9). `cross_store` distinguishes trigger-missing vs semantic-bypass vs broken-DDL. `security` calls the real production write paths with real model objects. `vault` isolates 24 invariants (per-item ERROR does not abort). Added `--selftest`. |
| `run_mutation_tests.py` | Corrected m2/m5/m6/m9/m10 so each reaches the real security path; m7 now lists a phantom file (MISSING). BLOCKED requires a structured semantic failure_code ∈ the mutation's allowed set AND hits_match (P0-1). Report carries started_at/ended_at/duration_ms/timed_out/signal, argv `detector_command`, normal-before/after raw logs+SHAs, selector self-test (P0-10). Default (replay) mode compares only a volatile-free `stable_signature` (P0-12). |
| `build_release.py` | Single build entry: every report derived from real subprocess results (no hardcoded PASS/counts). Per-step command/UTC/duration/exit/log-SHA. Manifest regenerated last, excluding `manifest.json`, `mutation_report.json`, and `evidence/`. `--package` emits the final ZIP. |
| `PROVENANCE.md` | Updated to R8; input R7 SHA, Stage B-R1 SHA, fixes, status. |
| `V1.3A.9_R7_TO_R8_DIFF.md` | This file (new). |
| `machine_test_report.json` / `security_invariants_report.json` / `restore_fault_injection_report.json` / `build_execution_report.json` / `change_evidence.json` / `manifest.json` | Regenerated from real build results. |

## Mutation matrix (R8) — every mutation is semantically valid and reaches its target

| Mutation | Detector | Semantic reached | Failure code |
|----------|----------|------------------|--------------|
| m1 | main_tests | repository field drift breaks suite | E-MAIN-TESTS-FAIL |
| m2 | cross_store | DDL valid, trigger present, cross-store write succeeds | E-CROSS-STORE-SEMANTIC-BYPASS |
| m3 | assertion_linter | test file SyntaxError | E-TEST-SYNTAX-ERROR |
| m4 | manifest | unlisted file | E-MANIFEST-UNLISTED |
| m5 | vault | staff/cross-store write authz bypassed | E-VAULT-WRITE-AUTH-BYPASS |
| m6 | cross_store | DDL loads, trigger fully absent | E-CROSS-STORE-TRIGGER-MISSING |
| m7 | manifest | manifest lists a non-existent file | E-MANIFEST-MISSING |
| m8 | assertion_linter | real assertion replaced by tautology | E-ASSERTION-TAUTOLOGY |
| m9 | vault | manager/cross-store rotate authz bypassed | E-VAULT-ROTATE-AUTH-BYPASS |
| m10 | vault | recovery Provider verification bypassed | E-RECOVERY-PROVIDER-VERIFY-BYPASS |
| m11 | vault | reconnect_count ≠ 1 | E-VAULT-RECONNECT-COUNT |
| m12 | assertion_linter | unique assertion replaced by tautology | E-ASSERTION-TAUTOLOGY |
| m13 | aad_class_binding | correct AAD fails / wrong AAD accepted | E-AAD-BINDING-BROKEN |
