# V1.3A.9 R6→R7 差异说明

## 核心变更

### P0-1: 执行完整性门禁
- 重写 `build_release.py`: 真实运行所有步骤, 从exit code生成报告
- 新增 `build_execution_report.json`: 所有步骤的真实exit code
- 禁止任何手工PASS结果

### P0-2: restore原子回滚修复
- `vault_repository.py`: 新增`_db_lock = threading.RLock()`共享锁
- `vault_recovery_service.py`: checkpoint失败硬阻断(E-RESTORE-CHECKPOINT)
- candidate封存复核: commit前重新计算SHA+counts, 不一致→E-RESTORE-CANDIDATE-CHANGED
- 同目录candidate/rollback: `tempfile.mkstemp(dir=db_parent)`
- 成功条件全部执法: integrity==ok AND fk==1 AND sha==candidate_sha AND counts==candidate_counts

### P0-3: 7项真实故障注入
- `tests/test_restore_fault_injection.py` 重写:
  - T1: InvalidTag预验证失败
  - T2: 备份文件不存在
  - T3: post-replace验证失败(行数不符)→回滚
  - T4: checkpoint失败→repo连接仍可用
  - T5: post-replace SHA不符→回滚
  - T6: checkpoint返回False
  - T7: candidate TOCTOU→E-RESTORE-CANDIDATE-CHANGED

### P0-4: mutation runner 13/13真实阻断
- `run_mutation_tests.py`: expected_hits参与判定(actual≠expected→ERROR)
- m3/m4/m7 expected=1(非0)
- m8 expected=1(唯一断言)
- m10/m11/m12语义修正
- m13 AAD类方法覆盖

### P0-5: mutation_report闭合
- `--write-report`写入ORIGIN+纳入Manifest
- 默认模式: 缺文件→ERROR; 字节比较

### 必改文件 (SHA全部变化)
- `app/daily_loop/services/vault_repository.py` — 共享锁
- `app/daily_loop/services/vault_recovery_service.py` — 三阶段restore
- `run_truth_gate.py` — 24项
- `run_mutation_tests.py` — expected_hits执法
- `tests/run_tests.py` — 适配
- `build_release.py` — 真实构建
- `tests/test_restore_fault_injection.py` — 7项

## 未修改范围
- 44份冻结合同、vendor、业务路由、生产环境
