# DM Daily Loop — 平台恢复环境变量示例
# 仅在恢复操作时使用, 日频业务进程不加载此文件
# 复制为 .env.recovery 并填入真实值, 禁止提交真实密钥

# 平台恢复签名密钥 — 独立于业务签名, 至少16字符, 禁止dev_前缀
DM_PLATFORM_RECOVERY_SIGNING_KEY=change_me_to_recovery_signing_key_at_least_16_chars

# 平台恢复口令 — 独立于业务service secret
DM_PLATFORM_RECOVERY_SECRET=change_me_to_platform_recovery_secret
